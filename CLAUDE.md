# Claude Code

@AGENTS.md

## Agent skills

### Issue tracker

Issues and PRDs live as GitHub issues in `EleosDesign/sidebar-demo-apr-2026`; skills use the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout — `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
