// Sales-owned content. Empty on main by design — sales-demo branch fills
// this in with client -> field rules (see CLAUDE.md client/session table).
// Keys must match EhrContext's clientName format ("Last, First").
export const CLIENT_LOCK_RULES = {};
