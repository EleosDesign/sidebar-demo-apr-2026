// Temporarily hidden demo options. Keep the underlying client and note-type data
// intact so these options can be re-enabled by removing their IDs from these sets.
export const HIDDEN_CLIENT_DROPDOWN_NAMES = new Set([
  'Trisha Platts',
]);

export const HIDDEN_TOOLBAR_NOTE_TYPE_IDS = new Set([
  'TreatmentPlan',
]);
